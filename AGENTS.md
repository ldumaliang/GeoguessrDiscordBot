# GeoGuessr Discord Bot Agent Notes

## Documentation Updates
- Update all README files when functionality, setup, usage, or output formatting changes.
- Keep simulate-related docs in sync with the test harness and sample data.

## Commands
- `npm run start` - Run the live bot.
- `npm run simulate` - Run the simulated test harness.
- `npm run lint` - Typecheck only.

## Git Safety
- Never push to a remote repository automatically.

## Simulate Harness
- Sample payloads live in `endpoint-samples/` and are used by `npm run simulate`.
- Update sample descriptions when the simulated output or inputs change.
